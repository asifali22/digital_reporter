### Commands
```
docker-compose -f docker-compose.develop.yml build
docker-compose -f docker-compose.develop.yml up

docker ps

docker exec -it container-id exec
tail -n 1000 -f /var/log/uwsgi/-->press tabs twice
```